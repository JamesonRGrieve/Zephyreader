# The Zephyrex framework stack (Storybook 8.6 + Next 16, linked source packages)
# carries peer ranges that npm's strict resolver rejects but that work in
# practice — the framework itself runs this combo under pnpm. Match that
# tolerance so installs resolve.
legacy-peer-deps=true
